<div class="navtitle">
  <a href="#" class="action-showgroups" title="Show all groups">
    <span data-icon="users-list"> </span>
    <span class="tooltip">Show all groups</span>
  </a>
  <h2 class="group-name">
    <a href="{{group_url}}" class="ajax-link group-dashboard" data-group-name="{{group_name}}" data-group-id="{{group_id}}" data-group-students-url="{{group_students_url}}" data-group-resource-url="{{group_resource_url}}" data-group-edit-url="{{group_edit_url}}" data-group-add-student-url="{{group_add_student_url}}" data-group-add-students-bulk-url="{{group_add_students_bulk_url}}" data-removed="{{removed}}">
      {{group_name}}
    </a>
  </h2>
</div>

<ul class="itemlist students-list">
  {{> student_list_items}}
</ul>

{{#if staff}}
<div class="additems">
  {{#if group_add_student_url}}<a href="{{group_add_student_url}}" class="action-addbulk ajax-link additem-village">Signup Parents</a>{{/if}}
</div>
{{/if}}
